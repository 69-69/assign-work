import 'package:flutter/material.dart';

class CustomTabModel {
  final String label;
  final IconData? icon;
  final String? tooltip;

  CustomTabModel({required this.label, this.icon, this.tooltip});
}